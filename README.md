Extract of kubernetes v1.32 documentation and parsed it into json.

NB: An empty reference = url. Specified reference is only used for clarification like /concepts#this-particular-concept.

1. Open the `docs-browser.html` in your browser.
2. Upload `summary.json` and `notes.json`

Supported functionnalities are:
* Code blocks
* Tables
* Images